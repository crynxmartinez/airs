import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata("Contact AIRS CRM | Request a Competitor Analysis", "Share your website and target market. Request a competitor analysis to find content gaps and get a practical website improvement plan.", "/contact");
export default function ContactLayout({ children }: { children: React.ReactNode }) { return children; }
